const form=document.getElementById('msgForm');const messages=document.getElementById('messages');if(messages)messages.scrollTop=messages.scrollHeight;function esc(s){return (s||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));}form?.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form);try{const r=await fetch('send_message.php',{method:'POST',body:fd});const d=await r.json();if(!d.ok) throw new Error(d.message||'Erreur');const txt=fd.get('contenu');let file='';if(d.fichier){file=`<a class="file" target="_blank" href="${esc(d.fichier.url)}">📎 ${esc(d.fichier.nom_original)}</a>`;}messages.insertAdjacentHTML('beforeend',`<div class="msg mine"><div class="avatar">ME</div><div class="bubble"><b>Moi</b>${txt?`<p>${esc(txt)}</p>`:''}${file}<time>À l'instant</time></div></div>`);form.reset();messages.scrollTop=messages.scrollHeight;}catch(err){alert(err.message);}});document.querySelector('input[type=file]')?.addEventListener('change',()=>{document.querySelector('textarea').placeholder='Fichier prêt. Clique sur Envoyer.'});document.getElementById('audioBtn')?.addEventListener('click',()=>alert('Démo audio : utilisez le bouton fichier pour envoyer un audio webm/mp3. La classe GestionFichier accepte les messages vocaux.'));
